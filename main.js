/*
const firstFunction =
  {
    message: {
      data: ['one', 'one2'],
    }
  }
const secondFunction =
  {
    message: {
      data: ['two', 'two2'],
    }
  }
  const thirdFunction =
  {
    message: {
      data: ['three', 'three2'],
    }
  }

const arr = [firstFunction, secondFunction, thirdFunction]

const randomArr = () => {
  let index = Math.floor(Math.random() * arr.length)
  let dataInd = Math.floor(Math.random() * 2)
  const james = arr[index].message.data[dataInd]
  console.log(james)
  
}

setInterval(randomArr, 2000)
*/
